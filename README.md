# Relatively functional film website
written in html, css and javascript only. No backend, just external api requests
# Has some bugs 
which can be avoided if you don't open too much links in new tabs
